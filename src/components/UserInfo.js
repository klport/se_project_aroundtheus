class Userinfo {
 constructor({ name, data }) {
  this._nameElement = document.querySelector(name);
  this._jobElement = document.querySelector(data);
 }
 getUserInfo() {
   return {
    
   }
 }

 setUserInfo{

 }

}